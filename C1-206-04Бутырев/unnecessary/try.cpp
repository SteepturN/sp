/*  BDV   */
#include "mlisp.h"
//________________ 
int main() {
 display( ( a ) );
	 newline();
	 display( ( -a ) );
	 newline();
	 display( ( +a ) );
	 newline();
	 display( ( 1.0 / a ) );
	 newline();
	 display( ( a + b ) );
	 newline();
	 display( ( a - b ) );
	 newline();
	 display( ( a / b ) );
	 newline();
	 display( ( a * b ) );
	 newline();
	 display( ( a + b + c + d ) );
	 newline();
	 display( ( a - ( b + c + d ) ) );
	 newline();
	 display( ( a * b * c * d ) );
	 newline();
	 display( ( a / ( b * c * d ) ) );
	 newline();
	 display( ( -( a + b ) ) );
	 newline();
	 display( ( 1.0 / ( a * b ) ) );
	 newline();
	 display( ( 1.0 / ( 1.0 / a ) ) );
	 newline();
	 std::cin.get();
	 return
  0;
	 }

